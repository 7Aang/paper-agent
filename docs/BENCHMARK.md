# Benchmark report

All values below are generated by repository scripts. The frozen retrieval labels are assistant-authored diagnostics and have **0 independently human-verified questions**, so these results are not a public benchmark.

## Retrieval (paper level)

12 papers / 993 chunks / 24 frozen questions.

| Method | Status | Hit@1 | Recall@3 | MRR | Mean latency |
|---|---|---:|---:|---:|---:|
| BM25 | COMPLETED | 0.833 | 1.000 | 0.910 | 0.42 ms |
| BM25 + title prior | COMPLETED | 0.833 | 1.000 | 0.910 | 0.61 ms |
| BM25 + title prior + query expansion | COMPLETED | 0.833 | 0.958 | 0.899 | 0.64 ms |
| Dense LSA (offline baseline) | COMPLETED | 0.875 | 1.000 | 0.938 | 0.43 ms |
| BM25 + Dense LSA (RRF) | COMPLETED | 0.917 | 1.000 | 0.958 | 1.14 ms |
| Sentence-Transformer dense | COMPLETED | 0.625 | 0.958 | 0.781 | 11.16 ms |
| BM25 + Sentence-Transformer (RRF) | COMPLETED | 0.875 | 0.958 | 0.925 | 10.65 ms |
| Hybrid + Cross-Encoder | COMPLETED | 1.000 | 1.000 | 1.000 | 901.59 ms |

Dense LSA is a reproducible TF-IDF + truncated-SVD baseline and is not a pretrained neural model. Pretrained MiniLM and Cross-Encoder results use locally downloaded public model artifacts.

## Evidence-gate ablation

| Arm | Target-paper coverage (4 answerable) | Refusal accuracy (4 unanswerable) |
|---|---:|---:|
| Gate OFF | 1.000 | 0.000 |
| Gate ON | 1.000 | 1.000 |

This deterministic gate only handles exact-detail questions with missing named constraints. Answer correctness remains NOT RUN because no independent semantic labels exist.

## Citation and reliability

- Exact-quote provenance: 24 returned claims; structural grounded rate 1.000. Semantic entailment: NOT RUN.
- Fault injection: 10/10 scenarios recovered (100.0%).
- Verifier ablation and multi-agent versus simplified pipeline: REQUIRES API KEY; historical runs are not silently reused as new results.

## Metric definitions

- **Hit@K:** fraction of questions with at least one relevant item in the first K results.
- **Recall@K:** fraction of all labeled relevant items recovered in the first K results, averaged across questions.
- **MRR:** mean reciprocal rank of the first relevant item.
- **nDCG@K:** rank-discounted relevance normalized by an ideal ordering.
- Paper-level metrics deduplicate chunks by paper ID. Chunk-level metrics require explicit chunk labels and are not reported for the current benchmark.

## Reproduction

```powershell
python scripts/validate_benchmark.py
python scripts/eval_retrieval.py --config configs/retrieval/bm25.yaml
python scripts/eval_retrieval.py --config configs/retrieval/dense_lsa.yaml
python scripts/eval_retrieval.py --config configs/retrieval/hybrid_lsa.yaml
python scripts/eval_agent.py --mode extractive --no-evidence-gate --output results/agent/extractive_gate_off.json
python scripts/eval_agent.py --mode extractive --output results/agent/extractive_gate_on.json
python scripts/eval_failures.py
python scripts/build_report.py
```
